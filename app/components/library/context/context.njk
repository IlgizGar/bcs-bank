{% import "../../../components/icon/icon.njk" as Icon %}

{% macro context( options ) %}
    <button class="context {{ options.class }}">
        <span>{{ options.title }}</span>
        {{ Icon.icon({'name': 'ver-explore'}) }}
    </button>
{% endmacro %}