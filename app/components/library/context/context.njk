{% import "../../../components/icon/icon.njk" as Icon %}

{% macro context( options ) %}
  {% if options.data %}
    {% set options = options.data %}
  {% endif %}
  {% set class_sep = joiner(' ') %}

  {% if options.attr %}
    {% set attr = '' %}
    {% set attr %}
      {% for key, value in options.attr -%}
        {{ class_sep() }} data-{{ key }} = "{{ value }}"
      {%- endfor %}
    {% endset %}
  {% endif %}

  {% if options.mods %}
    {% set mods = '' %}
    {% set mods %}
      {% for key, value in options.mods -%}
        {{ class_sep() }} context_{{ key }}-{{ value }}
      {%- endfor %}
    {% endset %}
  {% endif %}

  <div class="context js-context {{ options.class }} {{ mods | safe }}" {{ attr | safe }}>
    {% if options.items %}
      <select class="context__select" hidden name="{{ options.name }}_select">
        {% for item in options.items %}
          <option value={{ item.value }} {% if item.attr.prefix %}data-prefix="{{ item.attr.prefix | safe }}"{% endif %} {% if item.attr.title %}data-title="{{ item.attr.title | safe }}"{% endif %} {% if item.selected %}selected{% endif %}>{{ item.title }}</option>
        {% endfor %}
      </select>
    {% else %}
      <input type="text" hidden name="{{ options.name }}_input" value="{{ options.value }}">
    {% endif %}
    <button class="context__button" type="button">
        <span class="js-context-title">{{ options.title }}</span>
        {{ Icon.icon({'name': 'dropdown'}) }}
    </button>
  </div>
{% endmacro %}
