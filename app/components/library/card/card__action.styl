.card .card__action {
  display: flex
  align-items: center
  flex-shrink: 0

  height: 80px
  transition: all $interval
  border-top: solid 1px $color_borders
}

.card button.card__action {
  cursor: pointer
}
