.docs .docs__link
  display: inline-flex
  align-items: center

  margin-top: 15px
  margin-right: 24px

  height: 28px
  padding: 0 7px

  border-radius: 2px
  border: solid 1px $color_borders

  transition: all $interval

  span:first-child
    opensansBold(12px)
    color: rgba($color_elem, 0.6)

    &:after
      content: '•'
      margin: -1px 8px 0

      color: rgba($color_elem, 0.6)
      font-size: 9px;

  span:last-child
    opensansRegular(12px)
    color: rgba($color_elem, 0.6)
