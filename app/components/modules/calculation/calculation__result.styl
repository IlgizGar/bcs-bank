.calculation
  .calculation__thumbnail
    .calculation__result

      margin-top: 12px

      formular(40px)
      color: $color_elem
      @media(max-width 767px)
        margin-top: 8px
      .icon
        width: 40px
        height: 39px

        transform: translateY(2px)
